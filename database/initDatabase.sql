DROP database IF EXISTS `real_world_tdd`;
CREATE database `real_world_tdd`;
USE `real_world_tdd`;

CREATE TABLE IF NOT EXISTS real_world_tdd.toto (
    toto_id INT AUTO_INCREMENT,
    PRIMARY KEY (toto_id)
);
